---
# https://hub.docker.com/r/neilpang/acme.sh
services:
  acme:
    command: ["daemon"]
    container_name: acme
    env_file:
      - path: ./.env
        required: false
    image: neilpang/acme.sh:latest
    restart: unless-stopped
