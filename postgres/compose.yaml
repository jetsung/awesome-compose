---
# https://hub.docker.com/_/postgres

services:
  postgres:
    image: postgres:latest
    container_name: postgres
    hostname: postgres
    restart: unless-stopped
    env_file:
      - path: ./.env
        required: false
    ports:
      - 5432:5432
